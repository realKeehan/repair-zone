# Dependencies
node_modules/

# Environment / secrets
.env
.env.local
*.local

# Apache auth — never commit real credentials or a host-specific .htaccess
.htpasswd
/.htaccess

# Runtime data (live queue + inventory) — do not commit
data/db.json

# Logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
